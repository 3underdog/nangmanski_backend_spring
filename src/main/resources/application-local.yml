server:
  port: 8080
  servlet:
    context-path: /
  shutdown: graceful
  http:
    port: 8080

spring:
  config:
    activate:
      on-profile: local
  ############################################################
  # DataSource 설정
  ############################################################

#  session:
#    store-type: jdbc
#    jdbc:
#      initialize-schema: always

#  ############################################################
#  # Restful Client Configuration
#  ############################################################
#  restTemplate:
#    factory:
#      readTimeout: 60000
#      connectTimeout: 60000
#    httpClient:
#      maxConnTotal: 100
#      maxConnPerRoute: 5