server:
  port: 8080
  servlet:
    context-path: /
  shutdown: graceful
  http:
    port: 8080

spring:
  config:
    activate:
      on-profile: local
  ############################################################
  # DataSource 설정
  ############################################################\
  datasource:
    driver-class-name: name here
    url: url here
    username: username here
    password: password here
    connectionTimeout: 5000
    validationTimeout: 10000
    idleTimeout: 10000
    maxLifetime: 60000
    minimumIdle: 5
    maximumPoolSize: 10
  session:
    store-type: jdbc
#    jdbc:
#      initialize-schema: always

#  ############################################################
#  # Restful Client Configuration
#  ############################################################
#  restTemplate:
#    factory:
#      readTimeout: 60000
#      connectTimeout: 60000
#    httpClient:
#      maxConnTotal: 100
#      maxConnPerRoute: 5